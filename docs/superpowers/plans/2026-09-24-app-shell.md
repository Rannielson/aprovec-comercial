# Casca fixa (sidebar/topbar) do BFF (Fase 2a) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir a casca fixa (sidebar de navegação + topbar) fiel ao mockup aprovado e aplicá-la à única página de tenant autenticada que existe hoje (`/`), com navegação já prevendo os itens de fases futuras ("Minha carteira", "Fechamento") como indisponíveis.

**Architecture:** Um componente `Icon` novo (paths SVG exatos do mockup) e um componente `AppShell` novo (busca `/me`, renderiza sidebar+topbar+rodapé em torno de `{children}`) — `tenant-home.tsx` deixa de renderizar seu próprio cabeçalho e passa a ser conteúdo dentro de `<AppShell>`.

**Tech Stack:** Next.js 16 (App Router, Server Components), CSS puro em `globals.css`, Playwright para o E2E existente.

**Spec:** `docs/superpowers/specs/2026-09-24-app-shell-design.md`

## Global Constraints

- Zero endpoint novo, zero mudança em `src/Recorrencia.Api`/`src/Recorrencia.Db`, zero mudança em `web/src/lib/**`.
- Elementos só-demo do mockup (seletor "Explorar como", `demo-badge`, botão "Regras do demonstrativo", rótulo de papel do vendedor, contador no item Fechamento) **não são portados** — ver a spec para a lista completa e o porquê de cada um.
- A sidebar usa fundo **vermelho-escuro** (`--brand-red-dark`, `#ab090a`), não preto; o item ativo usa fundo preto (`--brand-black`) com raio assimétrico `14px 0 14px 0`.
- Novas classes CSS não podem colidir com classes genéricas já usadas por páginas fora de escopo (`platform-home.tsx` usa `.topbar`/`.shell` hoje) — usar nomes distintos onde há risco de colisão (ver Task 2).
- Responsivo em 2 estados apenas: desktop (sidebar fixa lateral, 224px) e `≤760px` (sidebar horizontal no topo).
- Textos da interface em português do Brasil.
- Commits: mensagens convencionais (`feat(web): ...`), terminando com a linha de atribuição indicada pelo ambiente.

---

### Task 1: Componente de ícones

**Files:**
- Create: `web/src/app/icon.tsx`

**Interfaces:**
- Consumes: nada (fundação).
- Produces: `Icon({ name, className }: { name: 'grid' | 'wallet' | 'calendar' | 'logout'; className?: string })` — componente React que renderiza um `<svg>`. A Task 2 importa e usa este componente.

- [ ] **Step 1: Criar o componente**

Crie `web/src/app/icon.tsx`:

```tsx
export type IconName = 'grid' | 'wallet' | 'calendar' | 'logout';

const svgProps = {
  viewBox: '0 0 24 24',
  fill: 'none',
  stroke: 'currentColor',
  strokeWidth: 1.65,
  strokeLinecap: 'round' as const,
  strokeLinejoin: 'round' as const,
  'aria-hidden': true,
} as const;

export function Icon({ name, className }: { name: IconName; className?: string }) {
  const cls = className ? `icon ${className}` : 'icon';
  switch (name) {
    case 'grid':
      return (
        <svg className={cls} {...svgProps}>
          <rect x={3} y={3} width={7} height={7} rx={1.4} />
          <rect x={14} y={3} width={7} height={7} rx={1.4} />
          <rect x={3} y={14} width={7} height={7} rx={1.4} />
          <rect x={14} y={14} width={7} height={7} rx={1.4} />
        </svg>
      );
    case 'wallet':
      return (
        <svg className={cls} {...svgProps}>
          <path d="M20 8V6a2 2 0 0 0-2-2H6a3 3 0 0 0 0 6h14v10H6a3 3 0 0 1-3-3V7" />
          <path d="M20 12h-5v4h5" />
          <path d="M16 14h.01" />
        </svg>
      );
    case 'calendar':
      return (
        <svg className={cls} {...svgProps}>
          <rect x={3} y={5} width={18} height={16} rx={2} />
          <path d="M16 3v4M8 3v4M3 11h18M8 15h2M14 15h2" />
        </svg>
      );
    case 'logout':
      return (
        <svg className={cls} {...svgProps}>
          <path d="M9 3H4v18h5M13 7l5 5-5 5M8 12h13" />
        </svg>
      );
  }
}
```

Os quatro `d`/coordenadas dos paths são copiados literalmente de `mockup/app.js:16-42` (chaves `grid`, `wallet`, `calendar`, `logout` do objeto `paths`), só reescritos como atributos JSX em vez de string SVG crua.

- [ ] **Step 2: Verificar tipos, lint e build**

Run: `cd web && npm run typecheck && npm run lint && npm run build`
Expected: sem erros. (O componente ainda não é usado por nada — isso só confirma que compila.)

- [ ] **Step 3: Commit**

```bash
git add web/src/app/icon.tsx
git commit -m "feat(web): add Icon component for the app shell"
```

---

### Task 2: Componente `AppShell` e CSS da casca

**Files:**
- Create: `web/src/app/app-shell.tsx`
- Modify: `web/src/app/globals.css`

**Interfaces:**
- Consumes: `Icon` (Task 1, `./icon`), `apiFetch` (`@/lib/api`), `Me` (`@/lib/types`).
- Produces: `AppShell({ active, children }: { active: ActivePage; children: React.ReactNode })` e `type ActivePage = 'overview' | 'wallet' | 'closing'`, ambos exportados de `web/src/app/app-shell.tsx`. A Task 3 importa e usa ambos.

**Por que uma classe nova para o topbar (`.shell-topbar`, não `.topbar`):** `platform-home.tsx` (fora de escopo desta fase) já usa a classe genérica `.topbar` existente para seu próprio cabeçalho simples. Se a casca reaproveitasse esse nome para o cabeçalho fixo (com altura, borda e padding diferentes), o cabeçalho da página de plataforma mudaria de aparência sem nenhuma task ter tocado nela. Por isso a casca usa nomes novos e distintos (`.app-shell`, `.sidebar`, `.workspace`, `.shell-topbar`, etc.) — a classe `.topbar` genérica continua existindo, sem mudança, para quem já a usa.

**Por que o botão "Sair" fica fora de `.sidebar-bottom`:** no mockup, `.sidebar-bottom` (nota de marca + bloco do vendedor) é ocultado em telas ≤760px (`display:none` — ver `styles.css`, breakpoint mobile). Isso é aceitável no mockup porque esse bloco é só decorativo lá. Mas o app real precisa de um jeito de sair da conta em qualquer largura de tela — por isso o formulário de logout é um elemento irmão de `.sidebar-bottom`, fora dele, e permanece visível mesmo quando `.sidebar-bottom` é escondido no mobile.

- [ ] **Step 1: Adicionar a CSS da casca ao `globals.css`**

Adicione ao final de `web/src/app/globals.css`:

```css
.app-shell { display: flex; min-height: 100vh; }
.sidebar { width: 224px; flex-shrink: 0; background: var(--brand-red-dark); color: #fff; display: flex; flex-direction: column; padding: 32px 16px 0; position: fixed; inset: 0 auto 0 0; z-index: 20; }
.brand { padding: 0 11px 34px; }
.brand img { width: 173px; height: auto; display: block; }
.brand-product { font-size: 0.55rem; letter-spacing: 0.09em; color: #f2c4c2; margin-top: 17px; white-space: nowrap; text-transform: uppercase; }
.sidebar nav { display: flex; flex-direction: column; gap: 8px; }

.icon { width: 20px; height: 20px; flex-shrink: 0; vertical-align: middle; }

.nav-item { display: flex; align-items: center; gap: 13px; width: 100%; border: 0; background: none; color: #fff0ee; text-align: left; min-height: 47px; padding: 0 15px; border-radius: 7px; font-size: 0.8rem; font-weight: 500; text-decoration: none; }
.nav-item:hover { background: #940000; color: #fff; }
.nav-item.active { background: var(--brand-black); color: #fff; border-radius: 14px 0 14px 0; }
.nav-item.disabled { color: #f4cdca; cursor: default; opacity: 0.85; }
.nav-item .icon { width: 19px; height: 19px; }
.nav-soon { margin-left: auto; background: #fff; color: #9d1111; border-radius: 5px; font-size: 0.6rem; padding: 2px 6px; white-space: nowrap; }

.sidebar-bottom { margin-top: auto; }
.sidebar-note { padding: 30px 16px 10px; font-size: 0.7rem; line-height: 1.9; color: #f4cdca; }
.sidebar-note strong { font-weight: 400; color: #fff; }
.seller { border-top: 1px solid #ffffff36; display: flex; align-items: center; gap: 10px; padding: 20px 10px 10px; }
.avatar { width: 38px; height: 38px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; background: #ffffff14; border: 1px solid #ffffff40; color: #fff; font-weight: 600; font-size: 0.7rem; flex-shrink: 0; }
.seller strong { display: block; font-size: 0.7rem; color: #fff; font-weight: 500; }
.seller-dot { height: 6px; width: 6px; border-radius: 50%; background: #fff; margin-left: auto; }

.seller-logout { padding: 10px 10px 24px; }
.seller-logout button { all: unset; display: inline-flex; align-items: center; gap: 6px; cursor: pointer; font-size: 0.7rem; color: #f4cdca; }
.seller-logout button:hover { color: #fff; }
.seller-logout .icon { width: 14px; height: 14px; }

.workspace { margin-left: 224px; min-width: 0; flex: 1; display: flex; flex-direction: column; }
.shell-topbar { height: 68px; border-bottom: 1px solid var(--line); display: flex; align-items: center; padding: 0 34px; background: var(--surface); flex-shrink: 0; }
.breadcrumb { display: flex; gap: 12px; font-size: 0.75rem; color: var(--muted); }
.breadcrumb > span { color: #b7b2b2; }
.breadcrumb strong { font-weight: 500; color: #333338; }
.workspace > main { padding: 33px 34px 27px; max-width: 1550px; margin: 0 auto; width: 100%; flex: 1; }
.app-footer { max-width: 1550px; margin: 0 auto; padding: 12px 34px 24px; font-size: 0.65rem; color: #8b94a4; width: 100%; }

@media (max-width: 760px) {
  .app-shell { flex-direction: column; }
  .sidebar { position: relative; inset: auto; width: 100%; padding: 15px 16px 10px; flex-direction: row; align-items: center; flex-wrap: wrap; }
  .brand { padding: 0; display: flex; align-items: center; gap: 12px; width: 100%; }
  .sidebar nav { flex-direction: row; width: 100%; margin-top: 12px; gap: 4px; }
  .nav-item { flex: 1; justify-content: center; min-height: 42px; font-size: 0.7rem; padding: 0 7px; }
  .nav-soon { display: none; }
  .sidebar-bottom { display: none; }
  .seller-logout { width: 100%; text-align: center; padding: 8px 0 4px; }
  .seller-logout button { justify-content: center; width: 100%; }
  .workspace { margin-left: 0; }
  .shell-topbar { height: 49px; padding: 0 20px; }
  .workspace > main { padding: 25px 20px 18px; }
}
```

- [ ] **Step 2: Criar o componente `AppShell`**

Crie `web/src/app/app-shell.tsx`:

```tsx
import Link from 'next/link';
import { apiFetch } from '@/lib/api';
import { Icon, type IconName } from './icon';
import type { Me } from '@/lib/types';

export type ActivePage = 'overview' | 'wallet' | 'closing';

const NAV_ITEMS: { id: ActivePage; icon: IconName; label: string; href: string | null }[] = [
  { id: 'overview', icon: 'grid', label: 'Minha recorrência', href: '/' },
  { id: 'wallet', icon: 'wallet', label: 'Minha carteira', href: null },
  { id: 'closing', icon: 'calendar', label: 'Fechamento', href: null },
];

export async function AppShell({ active, children }: { active: ActivePage; children: React.ReactNode }) {
  const me = await apiFetch<Me>('/me');
  const initials = me.name.split(' ').slice(0, 2).map((part) => part[0]).join('');
  const activeItem = NAV_ITEMS.find((item) => item.id === active);

  return (
    <div className="app-shell">
      <aside className="sidebar">
        <div className="brand">
          <img src="/logo-aprovec.webp" alt="APROVEC Brasil" width={173} height={40} />
          <div className="brand-product">Recorrência comercial</div>
        </div>
        <nav aria-label="Navegação principal">
          {NAV_ITEMS.map((item) =>
            item.href ? (
              <Link key={item.id} href={item.href} className={item.id === active ? 'nav-item active' : 'nav-item'}>
                <Icon name={item.icon} />
                <span>{item.label}</span>
              </Link>
            ) : (
              <span key={item.id} className="nav-item disabled">
                <Icon name={item.icon} />
                <span>{item.label}</span>
                <span className="nav-soon">Em breve</span>
              </span>
            ),
          )}
        </nav>
        <div className="sidebar-bottom">
          <div className="sidebar-note">
            Cada pagamento.
            <br />
            <strong>Um resultado que cresce.</strong>
          </div>
          <div className="seller">
            <span className="avatar">{initials}</span>
            <div>
              <strong>{me.name}</strong>
            </div>
            <span className="seller-dot" aria-label="Sessão ativa" />
          </div>
        </div>
        <form action="/logout" method="post" className="seller-logout">
          <button type="submit">
            <Icon name="logout" />
            Sair
          </button>
        </form>
      </aside>
      <div className="workspace">
        <header className="shell-topbar">
          <div className="breadcrumb">
            Comercial <span>/</span> <strong>{activeItem?.label ?? ''}</strong>
          </div>
        </header>
        <main>{children}</main>
        <footer className="app-footer">
          <span>APROVEC Brasil <span>·</span> Recorrência comercial</span>
        </footer>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Verificar tipos, lint e build**

Run: `cd web && npm run typecheck && npm run lint && npm run build`
Expected: sem erros. (`AppShell` ainda não é usado por nenhuma página — isso só confirma que compila. `tenant-home.tsx` continua com o cabeçalho antigo até a Task 3.)

- [ ] **Step 4: Commit**

```bash
git add web/src/app/app-shell.tsx web/src/app/globals.css
git commit -m "feat(web): add AppShell component with sidebar/topbar CSS"
```

---

### Task 3: Encaixar a página inicial na casca e ajustar o E2E

**Files:**
- Modify: `web/src/app/tenant-home.tsx`
- Modify: `web/e2e/fundacao.spec.ts`

**Interfaces:**
- Consumes: `AppShell` (Task 2, `./app-shell`).
- Produces: nenhuma interface nova (última task desta fase).

- [ ] **Step 1: Reescrever `web/src/app/tenant-home.tsx`**

Substitua o arquivo inteiro por:

```tsx
import { AppShell } from './app-shell';
import { apiFetch } from '@/lib/api';
import {
  currentCompetencia,
  formatCompetencia,
  formatMoney,
  formatPercent,
  isCompetencia,
  previousCompetencia,
  ruleLabel,
  statusBadgeClass,
  statusLabels,
} from '@/lib/format';
import type { Commissions, Me } from '@/lib/types';

export async function TenantHome({ competencia }: { competencia?: string }) {
  const me = await apiFetch<Me>('/me');
  const month = isCompetencia(competencia) ? competencia : currentCompetencia();
  const canSeeCommissions = me.permissions.some((p) => p.key === 'comissoes.visualizar');
  const [commissions, previous] = await Promise.all([
    canSeeCommissions ? apiFetch<Commissions>(`/commissions/${month}`) : Promise.resolve(null),
    canSeeCommissions
      ? apiFetch<Commissions>(`/commissions/${previousCompetencia(month)}`).catch(() => null)
      : Promise.resolve(null),
  ]);
  const own = commissions?.beneficiaries.find((b) => b.userId === me.id) ?? null;
  const previousOwn = previous?.beneficiaries.find((b) => b.userId === me.id) ?? null;

  return (
    <AppShell active="overview">
      <div className="shell">
        <div className="page-heading">
          <p className="eyebrow">Painel do vendedor</p>
          <h1>Minha recorrência</h1>
          <p className="muted">Acompanhe sua carteira e a origem de cada comissão.</p>
        </div>

        {commissions ? (
          <>
            <section className="commission-band">
              <div className="commission-total">
                <span className="overline">Comissão apurada · {formatCompetencia(month)}</span>
                <div className="hero-number">{formatMoney(own?.total ?? 0)}</div>
                <span className="subtle">Sobre pagamentos confirmados</span>
              </div>
              {own?.byRule.map((rule, index) => (
                <div className="commission-part" key={`${rule.ruleType}-${rule.level}-${rule.groupId}-${rule.rate}`}>
                  <div className={index === 0 ? 'rate-badge' : 'rate-badge secondary'}>
                    {new Intl.NumberFormat('pt-BR', { maximumFractionDigits: 2 }).format(rule.rate * 100)}
                    <span>%</span>
                  </div>
                  <div>
                    <span className="subtle">{ruleLabel(rule)}</span>
                    <strong>{formatMoney(rule.amount)}</strong>
                    <span className="formula">{formatMoney(rule.base)} · {formatPercent(rule.rate)}</span>
                  </div>
                </div>
              ))}
              <span className={statusBadgeClass(commissions.status)}>{statusLabels[commissions.status] ?? commissions.status}</span>
            </section>

            <form className="inline" method="get">
              <label>
                Competência
                <input type="month" name="competencia" defaultValue={month} />
              </label>
              <button type="submit" className="secondary">Ver</button>
            </form>

            {previous && previousOwn && previousOwn.total > 0 && (
              <section className="closing-card">
                <p className="closing-eyebrow">Fechamento anterior</p>
                <h2>{formatCompetencia(previousCompetencia(month))}</h2>
                <p className="closing-amount">{formatMoney(previousOwn.total)}</p>
                <span className={statusBadgeClass(previous.status)}>{statusLabels[previous.status] ?? previous.status}</span>
              </section>
            )}

            {commissions.beneficiaries.length === 0 ? (
              <p className="muted">Nenhuma comissão nesta competência.</p>
            ) : (
              <div className="table-wrap">
                <table>
                  <thead>
                    <tr>
                      <th>Pessoa</th>
                      <th>Origem</th>
                      <th className="number">Taxa</th>
                      <th className="number">Base paga</th>
                      <th className="number">Comissão</th>
                    </tr>
                  </thead>
                  {commissions.beneficiaries.map((person) => (
                    <tbody key={person.userId}>
                      {person.byRule.map((rule, index) => (
                        <tr key={`${rule.ruleType}-${rule.level}-${rule.groupId}-${rule.rate}`}>
                          <td>{index === 0 ? (person.name ?? 'Participante') : ''}</td>
                          <td>{ruleLabel(rule)}</td>
                          <td className="number">{formatPercent(rule.rate)}</td>
                          <td className="number">{formatMoney(rule.base)}</td>
                          <td className="number">{formatMoney(rule.amount)}</td>
                        </tr>
                      ))}
                      {person.byRule.length > 1 && (
                        <tr className="subtotal">
                          <td />
                          <td colSpan={3}>Total</td>
                          <td className="number">{formatMoney(person.total)}</td>
                        </tr>
                      )}
                    </tbody>
                  ))}
                </table>
              </div>
            )}
          </>
        ) : (
          <section className="card">
            <p className="muted">Seu perfil não inclui acesso às comissões.</p>
          </section>
        )}
      </div>
    </AppShell>
  );
}
```

- [ ] **Step 2: Adicionar a CSS do título de página ao `globals.css`**

Adicione ao final de `web/src/app/globals.css`:

```css
.page-heading { display: grid; gap: 6px; }
```

- [ ] **Step 3: Verificar tipos, lint e build**

Run: `cd web && npm run typecheck && npm run lint && npm run build`
Expected: sem erros.

- [ ] **Step 4: Corrigir as duas asserções do E2E que dependiam do cabeçalho antigo**

`web/e2e/fundacao.spec.ts` tem duas asserções em `getByRole('heading', { name: 'Olá, ...' })` que quebram porque o `<h1>` da página inicial não é mais o cumprimento — é "Minha recorrência" (ver Task 1, Step 1 desta task). O nome de quem está logado agora aparece na sidebar (`.seller strong`), não em um heading.

Na função de teste `'consultor entra, vê a comissão de setembro e sai'` (linha 27), troque:

```ts
  await expect(page.getByRole('heading', { name: 'Olá, João Silva' })).toBeVisible();
```

por:

```ts
  await expect(page.locator('.seller strong')).toHaveText('João Silva');
```

Na função de teste `'plataforma cria empresa e a administradora define a senha pelo convite'` (linha 82), troque:

```ts
  await expect(page.getByRole('heading', { name: 'Olá, Dona E2E' })).toBeVisible();
```

por:

```ts
  await expect(page.locator('.seller strong')).toHaveText('Dona E2E');
```

Nenhuma outra linha do arquivo precisa mudar (as asserções em `.hero-number`, `getByRole('button', { name: 'Sair' })`, etc. continuam válidas — o botão "Sair" tem o mesmo texto/papel, só mudou de posição na tela).

- [ ] **Step 5: Rodar o E2E**

Com o ambiente preparado (ver README ou `web/e2e/prepare.mjs`):

```bash
cd web && npm run e2e
```

Expected: os 5 cenários passam, incluindo os dois ajustados no Step 4.

- [ ] **Step 6: Verificar manualmente contra o mockup**

Com `joao@aprovec.local` / `senha-dev-123` em `http://aprovec.localhost:3000/`, confirme: sidebar vermelho-escura fixa à esquerda com o logo, 3 itens de navegação ("Minha recorrência" ativo em preto com canto assimétrico; "Minha carteira" e "Fechamento" com a etiqueta "Em breve", não clicáveis), nome "João Silva" no bloco do vendedor, botão "Sair" funcional. Título da página "Minha recorrência" (não mais "Olá, João Silva"). Reduza a largura da janela para menos de 760px e confirme que a sidebar vira uma barra horizontal no topo, a nota de marca/bloco do vendedor desaparecem, mas o botão "Sair" continua visível e funcional. Compare a sidebar lado a lado com o markup em `mockup/app.js:197` (fundo vermelho-escuro, não preto).

- [ ] **Step 7: Commit**

```bash
git add web/src/app/tenant-home.tsx web/src/app/globals.css web/e2e/fundacao.spec.ts
git commit -m "feat(web): wrap tenant home in the app shell"
```
